#include <iostream>

int main()
{
    /* 1st Problem
     
        double d1 = 5.7, d2 = 8.4, prod;
        prod = d1 * d2;

        printf("The product of D1 and D2 = %f", prod);
    */

    /* 2nd Problem
   *
       double pi = 3.141, radius = 1.5, area, height = 8.0;
       area = (2.0 * pi * radius * height) + (2.0 * pi * pow(radius, 2));

       printf("The area of the cylinder = %f", area);
   */

    /* 3rd Problem
    double wavelength, c = 3.0 * pow(10.0, 10.0);
    int f = 1000;

    wavelength = c / f;

    printf("The wavelength = %f", wavelength);

    */

    return 0;
}
